#### Useful docker commands
```bash
* start containers in background:
    docker-compose up -d
* stop containers:
    docker-compose down 
* list down containers:
    docker-compose ps
* Build or Rebuild Containers:
    docker-compose build
* Build and start Containers:
    docker-compose up --build
```
